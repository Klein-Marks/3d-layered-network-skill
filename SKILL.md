---
name: 3d-layered-network
description: 3D 分层功能网络可视化 skill。把分层系统、分叉-汇合架构、流水线呈现为可交互的单文件 HTML：层沿 Z 轴、组内径向分布、主节点点击展开子节点、边逻辑词按数据流分级标注、双主题（黑夜/白天米黄）。支持螺旋/分叉-汇合/线性三种布局模式。触发词：3D 网络、分层网络、功能呈现网络、workflow-3d、主节点展开、逻辑词连线、分叉汇合、思维导图 3D。
version: 2.0.0
---

# 3D 分层功能网络

单文件 HTML，把"分层系统 + 功能节点 + 关联关系"呈现为可交互 3D 网络。本质是**升级版思维导图**：从一个根节点生发出分支，分支再细分为子节点，跨分支的关联用带逻辑词的边连接。

参考实现：
- 螺旋布局：`examples/workflow-network.html`（六层工作流全景）
- 分叉-汇合布局：`examples/branch-merge-network.html`（双分支并行架构）

## 适用场景

- 分层架构全景（如六层工作流：编排/记忆/技能/上下文/成本/治理）
- **分叉-汇合架构**（如双管线并行：输入 -> 左右分叉 -> 验证汇合 -> 输出）
- 功能点很多（20+）且天然分层或分叉的系统
- 需要"先看骨架（主节点）、点击展开细节（子节点）"的渐进式呈现

## 设计流程（六步）

### 1. 定拓扑类型

在开始前先判断系统的逻辑拓扑，选择布局模式：

| 拓扑类型 | 特征 | 布局模式 | 示例 |
|---------|------|---------|------|
| **纯分层** | 每层顺序依赖，无分叉 | 螺旋式（默认） | 六层工作流 |
| **分叉-汇合** | 从根节点分出并行分支，在下游汇合 | 分叉-汇合式 | 双管线并行架构 |
| **线性流水线** | 单链路顺序处理 | 线性式（z 递增，XY 居中） | 信号处理七步管线 |

### 2. 归层与分叉

把全部功能节点归并到 N 层。每层一个主节点（层核心），主节点初始可见；子节点默认收起。

**分叉-汇合拓扑的关键**：多个主节点可共享同一 z 值（同层并行），通过 XY 偏移区分左右分支。例如：
```
L1 输入    z=0  x=0    (根节点居中)
L2 分支A   z=2  x=-170 (左分支)
L3 分支B   z=2  x=+170 (右分支，与 L2 同 z)
L4 汇合    z=4  x=0    (汇合节点居中)
L5 处理    z=6  x=0
L6 输出    z=8  x=0
```

### 3. 定逻辑词（三层分级规则）

不是所有边都需要逻辑词。按数据流分级：

| 边类型 | 是否加逻辑词 | 逻辑词风格 | 示例 |
|--------|------------|-----------|------|
| **主干数据流**（层间） | 加 | 简短动词（2-4 字） | "直接解码""送验""验证后融合""输出" |
| **承载边**（主节点->子节点） | **不加** | w="" 空字符串 | 展开时显示连线但不显示标签 |
| **子节点间数据流** | 仅实际数据流加 | 简短名词/动词 | "语义""参数""中间量""回放" |
| **非数据流关联** | **不加** | 不建边 | 纯逻辑关联不建边 |

**冗余路径去除原则**：如果 A->B->C 已有路径，则 A->C 直连边是冗余的，不建。例如：L2->L4->L5 已覆盖"分支A经验证后处理"，则不建 L2->L5 直连边。

### 4. 布局

**螺旋式**（适合纯分层）：
```js
const ang = (i / MAINS.length) * Math.PI * 2 - Math.PI / 2;
P[k] = {x: Math.cos(ang) * (30 + i * 10), y: Math.sin(ang) * (30 + i * 10), z: G[k].z * LAYER_SPACING};
```

**分叉-汇合式**（适合双分支/多分支）：
```js
const MAIN_POS = {
  L1:{x:0, y:0},      // 根节点居中
  L2:{x:-170, y:0},   // 左分支
  L3:{x:170, y:0},    // 右分支（与 L2 同 z）
  L4:{x:0, y:0},      // 汇合节点居中
  L5:{x:0, y:0},
  L6:{x:0, y:0}
};
MAINS.forEach(k => {
  const mp = MAIN_POS[k];
  P[k] = {x: mp.x, y: mp.y, z: G[k].z * LAYER_SPACING};
});
```

**子节点继承主节点 XY 偏移**（分叉布局关键）：
```js
N.forEach(n => {
  const mp = MAIN_POS[n.g] || {x:0, y:0};  // 继承主节点位置
  // 子节点围绕主节点位置（而非原点）做同心圆分布
  P[n.id] = {x: mp.x + Math.cos(angle) * r, y: mp.y + Math.sin(angle) * r, z: z};
});
```

### 5. 交互

点击主节点 toggle 展开/收起该层；点击任意节点右侧面板显示详情（功能/设计逻辑/架构作用/触发条件）；拖拽旋转、滚轮缩放、双击聚焦、自动旋转。

### 6. 双主题

黑夜（默认深底）+ 白天（米黄底），节点色双套，CSS 变量切换。

## 关键实现模式（必须遵守）

### 投影居中三件套（否则主体必然偏出屏幕）
```js
// 1) 动态云中心: 可见节点旋转后均值 (mx,my,mz)
// 2) persp 基准 = mz 而非 0: persp = PERSP/(PERSP + (rp.z - mz))
// 3) 屏幕包围盒居中: 求投影点 min/max 中心 (shX,shY), 所有元素减去它
```
- 绝对 z=0 做 persp 基准 -> 负 z 节点被放大 2-3 倍推挤出屏（血泪教训）
- 均值中心与包围盒中心在分布不对称时差 35px+，必须加包围盒平移

### 布局与渲染 gotcha
- scene 必须 CSS `position:absolute;inset:0`（父容器 relative），**JS 里禁止改 style.position**，否则高度塌陷为 0、cy=0、节点画到视口外
- 边 = 投影 div：`width=dist;transform:rotate(angle);transform-origin:0 50%`，z-index 取两端 depth 均值
- 节点 z-index 按 depth，边减半避让
- **主节点不能全放 XY 原点**：否则沿 Z 轴叠成一串，高层节点盖住低层，点击被拦截
  - 螺旋式：角度 = i/N*2π，半径随层递增
  - 分叉式：按拓扑声明 MAIN_POS，左右分支用 x 正负区分
- **子节点必须继承主节点 XY 偏移**：否则分叉布局下子节点全挤在原点周围，看不出分支归属
- 逻辑词标签：边中点 + 法线方向偏移 9px（nx=-dy/dist*9, ny=dx/dist*9），transform:translate(-50%,-50%) 保证文字水平可读，z-index 620
- **承载边标签渲染跳过**：`if(ed.w) { /* 渲染标签 */ }`，w="" 的承载边只画线不画标签
- 数据模型：主节点详情放 `G[k].info`（四段数组），子节点详情放独立 `INFO[id]` 四段数组，updatePanel 统一按 `isMain(id)` 取源

### 交互 gotcha
- `#scene, #scene *{user-select:none}`：否则拖拽旋转时文本被选中，观感崩坏
- 展开状态用 `Set`（expanded layers），边只在两端都可见时渲染
- 点击主节点 = toggle 展开 + 详情；点击空白 = 取消选中
- 子节点 span 设 pointer-events:none 后，Playwright 的 getByText().click() 命中测试会误判"元素被拦截"（真实用户点击无碍，事件冒泡到父 div 的 onclick）。自动化验证用 `span.parentElement.click()` 或直接调 toggleLayer()

### 双主题
- `:root` 黑夜变量 + `body.light` 覆盖（白天米黄底如 `#f5efe0`）
- 节点色双套数组 `COLORS.dark / COLORS.light`，切换时整体重渲
- 左侧渐变背景 `radial-gradient` 也要双套（按主题切换）

## 交付物

- 单文件 HTML（无外部依赖，离线可开）
- 标题用用户指定名
- 右侧解释框：功能 / 设计逻辑 / 架构作用 / 触发条件 四段
- 验证清单：
  1. 主节点初始可见、拓扑结构清晰（分叉/分层可辨识）
  2. 点击展开子节点 + 子节点围绕各自主节点分布
  3. 逻辑词仅出现在数据流边上，承载边无标签
  4. 主题切换后配色协调
  5. 任意视角下云居中（偏移 ≤2%）

## 布局与逻辑词演进

| 维度 | 初始设计 | 当前设计 |
|------|---------|---------|
| 布局模式 | 仅螺旋式 | 螺旋 + 分叉-汇合 + 线性（三选一） |
| 同层并行 | 不支持（每层 z 不同） | 支持（多主节点共享 z，XY 区分） |
| 逻辑词 | 每条边都加 | 三层分级：主干加/承载不加/子节点仅数据流加 |
| 冗余路径 | 无规则 | A->B->C 已有则不建 A->C |
| 子节点布局 | 围绕原点 | 继承主节点 XY 偏移 |
| 拓扑声明 | 无 | 第一步先定拓扑类型再布局 |
| 本质定位 | 分层网络 | 升级版思维导图（分叉-汇合树状结构） |

## 参考

- 理论：Sugiyama 分层图（IEEE TSMC 1981）、径向树、Hong & Nikolov 2005 3D 分层
- 螺旋布局演示：`examples/workflow-network.html`
- 分叉-汇合布局演示：`examples/branch-merge-network.html`
